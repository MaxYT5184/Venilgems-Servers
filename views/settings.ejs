<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Settings - Venilgem Services Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/dashboard">
                <i class="bi bi-robot"></i> Venilgem Services Dashboard
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard">
                            <i class="bi bi-speedometer2"></i> Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/users">
                            <i class="bi bi-people"></i> Users
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/settings">
                            <i class="bi bi-gear"></i> Settings
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logs">
                            <i class="bi bi-journal-text"></i> Logs
                        </a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">
                            <i class="bi bi-box-arrow-right"></i> Logout
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row">
            <div class="col-md-12">
                <h1>Settings</h1>
                <p>Configure your bot settings</p>
                
                <div class="card">
                    <div class="card-header">
                        <i class="bi bi-sliders"></i> Bot Configuration
                    </div>
                    <div class="card-body">
                        <form action="/settings" method="POST">
                            <div class="mb-3">
                                <label for="discordToken" class="form-label">Discord Token</label>
                                <input type="password" class="form-control" id="discordToken" name="discordToken" placeholder="Discord Bot Token" value="<%= typeof settings !== 'undefined' && settings.discordToken ? '********' : '' %>">
                                <div class="form-text">Your bot's Discord token (keep this secret)</div>
                            </div>
                            
                            <div class="mb-3">
                                <label for="clientId" class="form-label">Client ID</label>
                                <input type="text" class="form-control" id="clientId" name="clientId" placeholder="Client ID" value="<%= typeof settings !== 'undefined' && settings.clientId ? settings.clientId : '' %>">
                            </div>
                            
                            <div class="mb-3">
                                <label for="guildId" class="form-label">Guild ID</label>
                                <input type="text" class="form-control" id="guildId" name="guildId" placeholder="Guild ID" value="<%= typeof settings !== 'undefined' && settings.guildId ? settings.guildId : '' %>">
                            </div>
                            
                            <div class="mb-3">
                                <label for="logChannelId" class="form-label">Log Channel ID</label>
                                <input type="text" class="form-control" id="logChannelId" name="logChannelId" placeholder="Log Channel ID" value="<%= typeof settings !== 'undefined' && settings.logChannelId ? settings.logChannelId : '' %>">
                            </div>
                            
                            <div class="mb-3">
                                <label for="welcomeChannelId" class="form-label">Welcome Channel ID</label>
                                <input type="text" class="form-control" id="welcomeChannelId" name="welcomeChannelId" placeholder="Welcome Channel ID" value="<%= typeof settings !== 'undefined' && settings.welcomeChannelId ? settings.welcomeChannelId : '' %>">
                            </div>
                            
                            <div class="mb-3">
                                <label for="giveawayChannelId" class="form-label">Giveaway Channel ID</label>
                                <input type="text" class="form-control" id="giveawayChannelId" name="giveawayChannelId" placeholder="Giveaway Channel ID" value="<%= typeof settings !== 'undefined' && settings.giveawayChannelId ? settings.giveawayChannelId : '' %>">
                            </div>
                            
                            <div class="mb-3">
                                <label for="adminPassword" class="form-label">Admin Password</label>
                                <input type="password" class="form-control" id="adminPassword" name="adminPassword" placeholder="Admin Password" value="<%= typeof settings !== 'undefined' && settings.adminPassword ? '********' : '' %>">
                            </div>
                            
                            <div class="mb-3">
                                <label for="memberRoleId" class="form-label">Member Role ID</label>
                                <input type="text" class="form-control" id="memberRoleId" name="memberRoleId" placeholder="Member Role ID" value="<%= typeof settings !== 'undefined' && settings.memberRoleId ? settings.memberRoleId : '' %>">
                            </div>
                            
                            <div class="mb-3">
                                <label for="dashboardPort" class="form-label">Dashboard Port</label>
                                <input type="text" class="form-control" id="dashboardPort" name="dashboardPort" placeholder="Dashboard Port" value="<%= typeof settings !== 'undefined' && settings.dashboardPort ? settings.dashboardPort : '3000' %>">
                            </div>
                            
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-save"></i> Save Settings
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
